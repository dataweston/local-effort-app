# Archived Weekly Journal Routes

The `/weekly` and `/weekly/:slug` public journal routes were retired from production in favor of `/blog` and `/blog/:slug`.

These components are preserved only as implementation history. They are not imported by the active app or SSR prerender entry.
