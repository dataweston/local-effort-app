# Strategy checkpoints

Current execution checkpoint: `/CURRENT_WORK.md`.

This directory intentionally does not duplicate the capital master record. Financial truth remains in the current local-only Capital Master Record and Local Budget; `CURRENT_WORK.md` exists to preserve execution state between operators/agents.
